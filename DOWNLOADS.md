# Downloads

Standalone builds checked into this repository:

- [AddressEnrichment-windows.zip](downloads/AddressEnrichment-windows.zip) - Windows build containing `AddressEnrichment.exe`, `.env.example`, sample workbook, and README.

For new builds, prefer the GitHub Actions workflow in `.github/workflows/build-windows.yml` and upload the generated artifact here only when you want a fixed downloadable copy in git.
